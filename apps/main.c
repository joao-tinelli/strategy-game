#include <stdio.h>
#include "alianca.h"
#include "edificio.h"
#include "faccao.h"
#include "mensagens.h"
#include "unidade.h"

int main(int argc, char const *argv[])
{
    /*
    int n, m, fases; // tamanho do tabuleiro e numero de fases
    FILE *arquivo; // arquivo de entrada
    char linha[30];
    
    // Lê a primeira linha
    if (fscanf(arquivo, "%d %d", &n, &m) != 2){
        msg_erro("Erro ao ler a dimensao do mapa.", "fscanf");
        fclose(arquivo);
        return 1;
    }

    int mapa[n+5][m+5];

    // Lê a segunda linha
    if (fscanf(arquivo, "%d", &fases) != 1){
        msg_erro("Erro ao ler o nuemero de fases.", "fscanf");
        fclose(arquivo);
        return 1;
    }

    */
    



    return 0;
}
